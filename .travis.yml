language: csharp
mono: none
dotnet: latest
dist: trusty

os:
  - linux

script:
    - bash -c printenv | sort
    - dotnet --version
    - cd src 
    - dotnet restore
    - dotnet publish -c Release
    
  